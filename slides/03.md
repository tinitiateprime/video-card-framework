Duration: 5s
# Step 2 — Embeddings + Vector DB 🧠
![](https://picsum.photos/seed/rag3/1200/2000)

Convert each chunk into vectors:
- embedding(chunk_text) → numbers
- store vectors in a vector DB (or pgvector)

At query time:
- embedding(user_question)
- similarity search → top K chunks

---
Footer: Similar meaning → closer vectors • Slide 3/5
